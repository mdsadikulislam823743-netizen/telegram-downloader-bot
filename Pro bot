services:
  - type: worker
    name: telegram-bot
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y ffmpeg
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
